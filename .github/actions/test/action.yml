name: 'Test DocBox'
description: 'Runs our DocBox tests'

branding:
  icon: check-square
  color: green

runs:
  using: "composite"
  steps:
    - run: box install testbox
      shell: bash
    - run: box start
      shell: bash
    - run: box testbox run
      shell: bash