name: 'Test DocBox'
description: 'Runs our DocBox tests'

branding:
  icon: check-square
  color: green

runs:
  using: "composite"
  steps:
    - run: box install testbox
    - run: box start
    - run: box testbox run